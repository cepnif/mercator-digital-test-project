<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Generates a unique timestamp for log file names -->
    <timestamp key="timestamp" datePattern="yyyy-MM-dd_HH-mm-ss-SSS"/>

    <!-- Log pattern format -->
    <property name="LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"/>

    <!-- Log level configurable via environment -->
    <property name="LOG_LEVEL" value="${log.level:-INFO}" />

    <!-- Console appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- File appender: unique log file per test run -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/test-execution-${timestamp}.log</file>
        <append>true</append>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- Root logger -->
    <root level="${LOG_LEVEL}">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <!-- Reduce noise from external libraries -->
    <logger name="org.openqa.selenium" level="WARN"/>
    <logger name="org.apache.http" level="WARN"/>

</configuration>
